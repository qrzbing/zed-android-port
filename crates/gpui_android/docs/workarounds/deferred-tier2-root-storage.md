# Tier 2 root-only sdcard exec (deferred)

**Status:** Deferred

Settings-gated bind-mount /mnt/pass_through/0/emulated over ~/sdcard-exec for users who have root + Magisk and want to keep multi-GB projects on shared storage. Plan saved to memory; held back until the in-app Settings UI gets a toggleable Advanced section.

**Detailed writeup: TODO.** Stub created so the index links resolve.
