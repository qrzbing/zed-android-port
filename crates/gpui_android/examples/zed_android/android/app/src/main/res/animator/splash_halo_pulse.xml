<?xml version="1.0" encoding="utf-8"?>
<!-- Subtle scale pulse on the inner halo ring. Combined with the
     orb's own scale pulse this reads as a single breathing element
     at the icon's center. 1.8s period, accelerate/decelerate
     interpolator so the in/out feels organic. -->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:ordering="together">
    <objectAnimator
        android:propertyName="scaleX"
        android:valueFrom="1.0"
        android:valueTo="1.08"
        android:duration="1800"
        android:repeatCount="infinite"
        android:repeatMode="reverse"
        android:interpolator="@android:anim/accelerate_decelerate_interpolator" />
    <objectAnimator
        android:propertyName="scaleY"
        android:valueFrom="1.0"
        android:valueTo="1.08"
        android:duration="1800"
        android:repeatCount="infinite"
        android:repeatMode="reverse"
        android:interpolator="@android:anim/accelerate_decelerate_interpolator" />
</set>
